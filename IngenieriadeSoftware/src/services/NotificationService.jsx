// src/services/NotificationService.jsx
export const sendNotification = async (email, message) => {
    // Aquí puedes hacer una integración real con una API de correo o SMS
    console.log(`Enviando notificación a: ${email} con el mensaje: ${message}`);
  };
  